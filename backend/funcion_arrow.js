//SINTAXIS ARROW
//para escribir funciones
    /*REGLAS:
        1 si el bloque de codigo de la funcion flecha
        solo tiene una linea no hace falta poner
        corchetes 
        2 si la funcion flecha tiene un solo parametro
        se puede ommitir el parentesis de los parametros
        3 si el bloque de codigo de la funcion flecha
        solo tiene una linea, y esta linea debe retornar
        un valor, es posible omitir el return, pero solo 
        se aplica la regla 2*/

const saludo = (nombre) => `hola ${nombre}desde una funcion flecha`;

console.log (saludo ("jhosep"));

//para activarla o invocarla 

